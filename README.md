# 📌 PR Reviewer Service — README

## 🧾 Описание

Сервис для **автоматического назначения ревьюеров** на Pull Request’ы внутри команды, а также управления командами, пользователями и их активностью.  
Взаимодействие происходит **только через HTTP API**, спецификация — в файле `openapi.yml`.

Основные возможности:

- создание PR с **автоматическим назначением до 2 активных ревьюеров** из команды автора (автор исключается)  
- переназначение ревьюера с учетом команды и активности  
- получение списка PR, назначенных пользователю  
- управление командами и участниками (создание/обновление команд, изменение активности пользователей)  
- операция merge **идемпотентна** — повторный вызов возвращает актуальное состояние PR

## 🧱 Архитектура и стек

- Язык: Go (Go 1.25)  
- HTTP-фреймворк: Echo v4  
- БД: PostgreSQL 17 (используется как основное хранилище)  
- Драйвер БД: `pgx/v5`  
- Миграции: `goose`  
- Конфиг: `viper` (`config.yaml` + `.env`)  
- Логирование: `zap`  
- Контейнеризация: Docker, Docker Compose  

Ключевые решения:

- раздельные модели БД и DTO из OpenAPI (внешние `user_id` и `pull_request_id` хранятся как бизнес-ключи, внутренняя БД использует `BIGSERIAL` ключи для связей и производительности)  
- вся логика работы с БД инкапсулирована в слое `repository`  
- транзакции используются для операций, затрагивающих несколько таблиц (создание PR, переназначение ревьюера, массовые изменения)  

## ⚙️ Конфигурация

Конфигурация собирается из двух источников:

1. `config.yaml` — дефолтные значения (локальная разработка)  
2. `.env` — основные значения для запуска в контейнерах

Перед запуском:

```bash
cp .env.example .env
```

Минимально важные переменные в `.env`:

```env
# БД
DB_HOST=db
DB_PORT=5432
DB_USER=pr_user
DB_PASSWORD=pr_password
DB_NAME=pr_db
DB_SSLMODE=disable

# HTTP-сервер
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Логирование
LOG_LEVEL=info
LOG_FORMAT=json
```

Сервис **обязан слушать порт 8080** (по заданию), это обеспечивается комбинацией `SERVER_PORT=8080` и публикацией порта в `docker-compose.yml`.

## 🐳 Запуск через Docker Compose

Требования задания выполняются:  
- сервис и БД поднимаются командой одной команды  
- миграции применяются автоматически при старте сервисов
- сервис доступен на `http://localhost:8080`

### 1. Подготовка

```bash
cp .env.example .env
# при необходимости отредактировать значения (локальный порт БД и т.п.)
```

### 2. Сборка и запуск

```bash
docker-compose up --build
```

После успешного старта:

- PostgreSQL доступен внутри сети контейнеров по хосту `db:5432`  
- сервис доступен с хоста: `http://localhost:8080`  

### 3. Остановка

```bash
docker-compose down
```

## 📂 Структура проекта

Условная структура (важные директории):

- `cmd/app` — точка входа HTTP-сервиса  
- `internal/config` — загрузка и мердж `config.yaml` + `.env`  
- `internal/repository` — работа с PostgreSQL, все SQL-запросы, транзакции
- `internal/models` — описание OpenAPI-моделей 
- `internal/handlers` — хэндлеры, биндинг запросов/ответов к OpenAPI-моделям  
- `migrations` — миграции `goose` (создание таблиц, внешние ключи, индексы)  
- `openapi.yml` — спецификация API из задания  

## 🧠 Ключевые бизнес-механики

### Назначение ревьюеров при создании PR

- вход: `pull_request_id` (внешний ID), `pull_request_name`, `author_id` (внешний ID автора)  
- по `author_id` находится внутренний ID автора и его команда  
- из команды автора выбираются **до 2 случайных активных участников**, исключая автора  
- PR сохраняется в таблицу `pull_requests` (с внутренним `id` и внешним `external_id`)  
- связи с ревьюерами сохраняются в `pr_reviewers` через внутренний ID  
- в ответе возвращаются **внешние** `user_id` ревьюеров  

### Переназначение ревьюера

- проверяется, что PR не в статусе `MERGED`  
- проверяется, что старый ревьюер действительно назначен  
- выбирается новый кандидат: активный участник **из нужной команды**, не автор и не один из уже назначенных  
- вся операция выполняется в транзакции  

### Merge PR (идемпотентность)

- при первом вызове PR переводится в статус `MERGED`, проставляется `mergedAt`  
- повторные вызовы возвращают актуальное состояние PR  
- после `MERGED` операции переназначения ревьюеров запрещены  

## 🧪 Тестирование

Реализовано:

- сценарии E2E через HTTP REST Client (при наличии — через отдельный тестовый бинарь или `curl`/`k6` скрипты)

## ✅ Линтер и качество кода

- используется `golangci-lint` с конфигурацией в `.golangci.yml`  
- включены базовые линтеры: `govet`, `errcheck`, `staticcheck`, форматирование и style-check  
- линтер можно запустить командой:

```bash
golangci-lint run ./...
```

## 🧩 Принятые допущения

Примеры допущений, которые стоит явно указать:

- внешние идентификаторы пользователей (`user_id`) и PR (`pull_request_id`) считаются уникальными и неизменяемыми
- пользователь может состоять ровно в одной команде (схема БД это допускает)
- при отсутствии доступных активных ревьюеров PR создается без ревьюеров (соответствует заданию: 0/1/2 ревьюера)  
- при конфликтах по внешним ID возвращаются соответствующие ошибки (`409` с кодами `PR_EXISTS`, `TEAM_EXISTS` и т.п.)